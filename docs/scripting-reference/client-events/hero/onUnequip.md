---
title: 'onUnequip'
---
# `event` onUnequip <font size="4">(client-side)</font>
!!! info "Available since version: 0.3.0"

Triggered when the hero unequips an item.

## Parameters
```c++
void onUnequip(string item)
```

* `string` **item**: Item instance.
